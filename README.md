# CV-Analyzer
CV Analyzer AI is a web app that analyzes uploaded CVs using NLP. It generates a CV score and actionable feedback, including keyword optimization and job matching. Built with Django, it uses RabbitMQ and multithreading for fast processing, and Docker for scalable deployment.
